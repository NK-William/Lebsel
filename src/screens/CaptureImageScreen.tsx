import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CaptureImageScreen = () => {
  return (
    <View>
      <Text>CaptureImageScreen</Text>
    </View>
  );
};

export default CaptureImageScreen;

const styles = StyleSheet.create({});
